class Solution:
    def numTrees(self, n: int) -> int:
        dp = [1,1] + [0]*(n-1)
        for i in range(2, n + 1):
            for val in range(1, i +1):
                dp[i] +=dp[val - 1]*dp[i-val]
        return dp[-1]
